# Create some sample data
heights <- c(160, 170, 165, 155)
names <- c("Alice", "Bob", "Charlie", "David")

# Create a bar plot
barplot(heights, names.arg = names, xlab = "Names", ylab = "Height", main = "Height of Individuals")
